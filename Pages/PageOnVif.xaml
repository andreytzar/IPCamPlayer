<Page x:Class="IPCamPlayer.Pages.PageOnVif"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:IPCamPlayer.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageMain">
    <DockPanel Background="White">
        <TextBlock Text="Пікдлючення до OnVif IP камери" FontSize="18" DockPanel.Dock="Top"/>
        <Separator DockPanel.Dock="Top"/>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="3"/>
                <RowDefinition Height="150"/>
            </Grid.RowDefinitions>
            <Grid Background="LightGray">
                <Image Source="{Binding Image, Mode=OneWay}" Stretch="Uniform"/>
            </Grid>
            <GridSplitter HorizontalAlignment="Stretch" Background="Gray" Grid.Row="1"/>
            <Grid Grid.Row="2" Margin="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition  Width="*"/>
                    <ColumnDefinition  Width="auto"/>
                </Grid.ColumnDefinitions>
                <Grid Margin="3" Grid.ColumnSpan="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <ComboBox SelectedItem="{Binding Profile}" ItemsSource="{Binding Profiles}" DisplayMemberPath="Name" Grid.Column="0" MinWidth="100"/>
                    <TextBox Grid.Column="1" Text="{Binding Rtsp}" Margin="3,0"  Grid.ColumnSpan="1"/>
                    <StackPanel Orientation="Horizontal" Grid.Column="2">
                        <Button Command="{Binding BCPlay}" Content="Play" Background="{x:Null}" HorizontalAlignment="Left" Padding="2"/>
                        <Button Command="{Binding BCStop}" Content="Stop" Background="{x:Null}" Padding="2" Margin="3,0"/>
                    </StackPanel>
                    <Separator Background="Black" Grid.Row="1" Grid.ColumnSpan="10" Margin="5"/>
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>
                    <DockPanel Margin="3">
                        <TextBlock Text="Адреса камери: " DockPanel.Dock="Left"/>
                        <TextBox Text="{Binding Url}" DockPanel.Dock="Left"/>
                    </DockPanel>
                    <StackPanel  Orientation="Horizontal" Grid.Row="1" Margin="3">
                        <TextBlock Text="Логін: " VerticalAlignment="Center"/>
                        <TextBox Text="{Binding Login}" Width="100"/>
                        <TextBlock Text="Пароль:" Margin="5,0" VerticalAlignment="Center"/>
                        <PasswordBox x:Name="passBox" Width="100"/>
                        <ComboBox SelectedItem="{Binding Mode}" ItemsSource="{Binding Modes}" Width="100" Margin="5,0"/>
                        <ComboBox SelectedItem="{Binding CredentialType}" ItemsSource="{Binding CredentialTypes}" Width="100" />
                    </StackPanel>
                </Grid>
                <TextBox Grid.Row="2" Grid.ColumnSpan="10" Text="{Binding StatusOnvif}" TextWrapping="Wrap" Margin="3"/>
                <Button Content="Connect" Grid.Row="1" Grid.Column="1" Grid.RowSpan="10" Margin="5" Padding="3" FontSize="14" FontWeight="Bold" Background="{x:Null}" VerticalAlignment="Top"  Click="Connect_Click"/>
            </Grid>
        </Grid>
    </DockPanel>
</Page>
